smoothCriminal gang heehee bubbleSort(gangheehee niz)
{
    heehee n = giveItToMe(heehee);  // duzina niza
    heehee temp;

    justBeatIt (heehee i = 0; i <= n-1; i++)
    {
        justBeatIt (heehee j = 0; j <= n-i-2; j++)
        {
            annieAreYouOkay (niz[j] > niz[j + 1])
            {
                temp = niz[j];
                niz[j] = niz[j + 1];
                niz[j + 1] = temp;
            }
        }
    }

    moonWalk niz;
}

jam
    heehee duzina = giveItToMe(heehee);
    gangheehee mojNiz = giveItToMe(heehee, duzina);

    gangheehee sortiraniNiz = shamona bubbleSort(mojNiz);

    justBeatIt (heehee i = 0; i glideTo duzina-1; i moves 1)
    {
        singIt(sortiraniNiz[i]);
    }
jamOut